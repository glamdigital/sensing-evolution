<div class="header">
    <img class="nav-menu-button" id="nav-menu-button" src="img/menu.png" width=50>
    <span class="proximity-indicator">Scanning...</span>
    <h1 class="trail-title">{{trailTitle}}</h1>
</div>
<div class="topic">
    <h2>{{topicTitle}}</h2>
</div>

<div class="search-item">
    <div class="find-prompt">Can you find...</div>
    <img class="item-image" src="img/{{image}}">
</div>

<div class="item-title">
    <h3 class="name">{{title}}</h3>
</div>

<div class="hint-container">
    <a href="#" class="show-hint">Need a hint?</a>
    <smaller>({{beaconHint}} beacon)</smaller>
    <p class="hint">Hint text appears here</p>
</div>

<div class="found-item">
    <video id='foundVideo' width="100%" height="auto" webkit-playsinline controls preload="auto">
      <source src="video/{{video}}" type="video/mp4">
      <!--<source src="movie.ogg" type="video/ogg">-->
        Your browser does not support the video tag.
    </video>
    <h3>You found it!</h3>
    <a href="#/restart" class="restart-all">Restart</a>
</div>

<div class="question">
</div>